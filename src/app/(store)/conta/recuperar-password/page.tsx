import RecuperarPasswordClient from "./RecuperarPasswordClient";

export const metadata = { title: "Recuperar senha — MDTech Solutions" };

export default function RecuperarPasswordPage() {
  return <RecuperarPasswordClient />;
}
